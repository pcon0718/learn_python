# Pass an argument to the parameter in the function
def greet_user(username): #username is the paramenter
    """Display a simple greeting."""
    print(f"Hello, {username.title()}!")

greet_user('jesse') #'jesse' is an argument